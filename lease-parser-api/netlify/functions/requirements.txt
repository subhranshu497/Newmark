# Runtime deps for the Netlify Function only. Deliberately excludes
# confluent-kafka (unused at request time here, and its librdkafka C
# dependency isn't worth carrying into the function bundle) and
# uvicorn/alembic (dev-server/migration tools, not needed by the handler).
fastapi>=0.115
mangum>=0.17
sqlalchemy>=2.0
greenlet>=3.1
asyncpg>=0.29
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0
pybreaker>=1.2
anthropic>=0.34
openai>=1.40
boto3>=1.34
pypdf>=4.0
python-multipart>=0.0.9
